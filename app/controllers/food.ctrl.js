

(function(){
	var ang = angular.module("food_module", ["custom_directive"]) // Square brackets contains the list of dependent modules, declaring a module

	ang.controller("MenuController", function( $scope){
		
	});

})();